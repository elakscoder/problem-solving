#include<bits/stdc++.h>
using namespace std;
void explainUset()
{
    unordered_set<int> st;
    //does not stores in a sorted order
    //lower_bound and upper_bound function does not works,
    //rest all functions are same
    //has better complexity than set in most cases
    //except some when collision happens
    

}


int main()
{
    explainUset();
    return 0;
}